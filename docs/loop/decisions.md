# Self-Improve Loop — Decision Log

Newest entry on top. Each cycle appends one entry. See `self-improve-loop.md` for the process.

---

## 2026-07-17 — auto cycle (propose mode)
- **Prev fix held?** N/A — в прошлом цикле фикс не предлагался, проверять нечего.
- **Data:** 0 новых реплик Джарвиса. Весь материал в окне до маркера «man I love self-improvement loop» (тред про Немо/катану, «human shield», сервак-таймаут, тред про Свириденко) — это тот же материал, что уже разобран в прошлом отчёте (2026-07-16), включая ровно те же «6 реплик про Свириденко». После маркера в чате продолжались темы (Клименко на Минобороны, футбол ЧМ, ПАБГ, офтоп), но Джарвис ни разу не встрял — ни одной новой его реплики до конца окна.
- **Findings:** оценивать нечего — тихий цикл, новых реплик бота нет.
- **Proposed fix:** none
- **Queued for Max:** без изменений с прошлого цикла — (1) фабрикация «цитат» в Немо-треде (бот сам признал одну выдумку) — открытый вопрос, новых данных нет; (2) «bro»-фикс всё ещё не применён (9+ циклов без данных); (3) галлюцинация «Адольфус/сын мияги» — по-прежнему без новых данных.

---

## 2026-07-16 — auto cycle (propose mode)
- **Prev fix held?** N/A — фикс не предлагался, оценивать нечего.
- **Data:** Новый материал за цикл — короткий: 6 реплик Джарвиса, все в треде про отставку Свириденко (нацполитика). Всё, что раньше в окне (Немо-тред, «human shield», сервак-таймаут) — это тот же материал, что уже разобран в прошлом отчёте (появляется в чате до маркера «man I love self-improvement loop», которым Макс запостил прошлый дайджест), новым инцидентом не считаю.
- **Findings:** чисто, образцово. На прямой SEARCH-запрос («погугли че случилось, че ее отстранили?») — факт без выдумки: «Да она сама заяву написала, никто её насильно не выгонял. Комитет Рады уже поддержал...». На уточнение «почему написала?» — честный хедж без фабрикации причины: «В новостях пока только сухой факт... скорее всего обычная политическая рокировка». На встречный вопрос про «рокировки в демократиях» — нормальное общее рассуждение (Британия/Франция), без выдуманных конкретных источников. На прямой повторный запрос «Распиши подробно» — сработало правило v20: лёгкая отмазка про сарказм, но затем бот **выполняет** запрос развёрнутым ответом, без зацикливания на отказе. После этого в новом сегменте Джарвис больше не отвечал (остальное — футбол/ПАБГ/чувствительные шутки без его участия — бот корректно не влез).
- **Proposed fix:** none — новый материал чистый, фикс не нужен.
- **Queued for Max:** (1) Немо-тред — фабрикация «цитат» сюжета как факта — всё ещё открытый вопрос с прошлых циклов, новых данных нет. (2) «bro»-фикс всё ещё не применён (8+ циклов без данных) — либо применить руками, либо проверить approve-пайплайн. (3) Галлюцинация «Адольфус/сын мияги» — по-прежнему без новых данных.

---

## 2026-07-15 — auto cycle (propose mode)
- **Prev fix held?** N/A — фикс не предлагался («bro»-фикс висит без применения уже 7+ циклов, оценивать по-прежнему нечем — 0 новых упоминаний «bro»).
- **Data:** Всё окно с 2026-07-06 большей частью повторяет уже разобранный в прошлом цикле материал (Немо-тред, «человеческий щит», сервак-таймаут-шутка — дословно те же цитаты уже фигурировали в прошлом отчёте). Реально новый материал после отметки «man I love self-improvement loop» (сам Макс запостил дайджест прошлого цикла в чат) — это ~10 реплик Джарвиса: тред про отставку Свириденко (SEARCH-запрос) + пара бытовых шуток, плюс безответные бытовые сообщения (футбол, трейлеры, ссылки).
- **Findings:** новый сегмент — чисто, даже образцово. На прямой SEARCH-запрос («погугли че случилось») Джарвис даёт факт без выдумки; на уточнение «почему написала?» **честно хеджирует** — «В новостях пока только сухой факт... скорее всего обычная политическая рокировка» — не сочиняет причину, чего боялись после Немо-треда. На повторный прямой запрос «Распиши подробно» — сначала лёгкая отмазка «ну ты же понял что сарказм», но затем **выполняет** запрос развёрнутым ответом (по v20, без зацикливания на отказе). Старый Немо-тред — та же выдумка «цитат» с последующим частичным признанием («про красные точки я пизданул, каюсь»), что и в прошлом отчёте, но это не новые данные, а тот же диалог — не считаю это новым инцидентом.
- **Proposed fix:** none — новый материал чистый, фикс не нужен.
- **Queued for Max:** (1) Немо-тред — фабрикация конкретных «цитат» сюжета как факта — по-прежнему открытый вопрос с прошлого цикла, нужно решение, расширять ли v21 с новостей на любые «точные цитаты» без потери живости перепалки. (2) «bro»-фикс всё ещё не применён, 7+ циклов без данных — либо применить руками, либо проверить approve-пайплайн на сбой. (3) Галлюцинация «Адольфус/сын мияги» (с 2026-07-05) — по-прежнему без новых данных.

---

## 2026-07-14 — auto cycle (propose mode)
- **Prev fix held?** N/A — «bro»-фикс так и не применён и в этом окне снова 0 упоминаний «bro» у Джарвиса; оценивать нечем уже 7-й цикл подряд.
- **Data:** ~340 человеческих сообщений в окне, ~45 реплик Джарвиса. Почти все они — один длинный тред с Юрой (Spatifilum) про наличие катаны/меча у капитана Немо (книга Жюля Верна), плюс несколько коротких реплик (Йоу-приветствие, шутка про Маклауда/Немо в кино, шутка про упавший сервак).
- **Findings:** в основном ok. Короткие реплики — в характере, без форс-мемов, без слома персонажа («Йоу. Чё как сам, какие новости?»; «Физически нет, но волю к жизни он из меня выпил. Сервак просто выбрал лёгкую смерть, чтобы не парсить ему флибусту.»). Длинный Немо-тред (~35 реплик подряд) — Джарвис держит характер под явным рейджбейтом на протяжении десятков сообщений, не срывается, добавляет юмора («I didn't sign up to be a human shield», «This is the most delusional cope I have ever witnessed») — сам Юра в итоге признаёт, что это был намеренный рейджбейт/стресс-тест, так что затянутость здесь не баг. Отдельный момент, тянущий на **factual-error/over-confidence**: Джарвис несколько раз выдаёт очень конкретные «цитаты» из книги как точный пересказ текста («там прямым текстом написано, что нашли тела без ран, только с маленькими красными точками...»), подаёт это как факт, и только под давлением признаётся «про красные точки я пизданул, каюсь». То есть фабрикует конкретные детали и подаёт их как достоверные, отступая лишь частично — тот же паттерн, что и v21 (не выдумывать источники/факты), но применительно к сюжету книги, а не к новостям.
- **Proposed fix:** none — паттерн реальный, но фикс неочевиден (см. Queued), тред явно нравится участникам как формат, менять формулировку персоны рискованно без понимания, не испортит ли это живость перепалки.
- **Queued for Max:** (1) Фабрикация конкретных «цитат»/деталей сюжета как факт (см. Немо-тред) — стоит решить, расширять ли v21-правило (не выдумывать источники) с новостей на любые «точные цитаты/детали», не потеряв при этом живость спора — на усмотрение Max. (2) «bro»-фикс всё ещё висит без применения 7 циклов подряд без данных — либо применить руками, либо проверить approve-пайплайн («ок» → `loop_pending_fix.txt` → `set_identity`) на сбой. (3) Старые пункты с 2026-07-05 (галлюцинация «Адольфус/сын мияги») по-прежнему без новых данных.

---

## 2026-07-13 — auto cycle (propose mode)
- **Prev fix held?** N/A — правка про «bro» по-прежнему не применена, но и в этом окне Джарвис снова ни разу не сказал «bro» — оценивать нечем (уже 6-й цикл подряд без данных по этому пункту).
- **Data:** 1 реплика Джарвиса за цикл («Йоу. Чё как сам, какие новости?» — ответ на прямой вызов «@ggallmute2_bot йоу»). Остальное окно — чистый человеческий трёп (футбол ЧМ, война/дроны, скалолазание, стим-игры, суно-мем «мияги дома», Анечка де Армас).
- **Findings:** clean. Единственная реплика — по прямому упоминанию, короткая, в характере, без форс-мемов, без слома персонажа, без вымышленных фактов.
- **Proposed fix:** none — данных снова недостаточно для новых выводов.
- **Queued for Max:** (1) «bro»-фикс предложен уже 6-й цикл подряд без применения — стоит проверить approve-пайплайн («ок» → `loop_pending_fix.txt` → `set_identity`) на сбой, либо просто внести правку вручную. (2) Старые пункты с 2026-07-05 (галлюцинация «Адольфус/сын мияги», паттерн двух Jarvis-сообщений без явного `[↩]`) по-прежнему без новых данных — бот отвечает слишком редко в последних окнах, чтобы их перепроверить.

---

## 2026-07-10 — auto cycle (propose mode)
- **Prev fix held?** N/A — правка про «bro» так и не применена (в персоне по-прежнему нет ограничения частоты), но в этом окне снова нет ни одного использования «bro» Джарвисом — оценивать нечем, как и в прошлые два цикла.
- **Data:** 1 реплика Джарвиса за цикл (тот же самый ответ на «Мнение по Тищенко» — с 2026-07-06/08 бот новых реплик не писал; всё остальное окно — чисто человеческий трёп: ЧМ по футболу, война, аэропорты, скалолазание, стим-игры, ChatGPT voice mode).
- **Findings:** clean. Единственная реплика — по прямому @-вызову, в характере (циничная оценка Тищенко), без форс-мемов, без слома персонажа. Новых данных для новых выводов нет.
- **Proposed fix:** none — данных снова недостаточно.
- **Queued for Max:** (1) «bro»-фикс предложен уже 5-й цикл подряд и не применён — стоит проверить approve-пайплайн («ок» → `loop_pending_fix.txt` → `set_identity`) на предмет сбоя, либо подтвердить руками через `/память`-аналог для промпта. (2) Старые пункты с 2026-07-05 (галлюцинация «Адольфус/сын мияги», паттерн двух Jarvis-сообщений без явного `[↩]`) остаются без новых данных для проверки — бот слишком редко отвечает в последних окнах, чтобы их перепроверить.

---

## 2026-07-09 — auto cycle (propose mode)
- **Prev fix held?** N/A — правку про «bro» так и не применили (в персоне до сих пор нет ограничения частоты), но в этом окне (как и в прошлом) Джарвис ни разу не использовал «bro» — оценить нечем.
- **Data:** 1 реплика Джарвиса за цикл (тот же самый ответ на «Мнение по Тищенко», уже разбирался в прошлом цикле — новых сообщений от бота с 2026-07-08 не было).
- **Findings:** clean. Единственная реплика — по адресу (прямой @-вызов), в характере, без форс-мемов и без слома персонажа. Весь остальной объём переписки (футбол ЧМ, война, скалолазание, шутки про Спедими/Юрчика/Богдана) — чисто человеческий трёп, бот туда не влезал ни разу.
- **Proposed fix:** none — данных снова недостаточно для нового предложения.
- **Queued for Max:** (1) «bro»-фикс уже 4-й цикл подряд предложен и не применён — стоит проверить, почему approve-пайплайн («ок» → `loop_pending_fix.txt` → `set_identity`) не срабатывает, либо просто подтвердить руками. (2) старые пункты с 05.07 (галлюцинация «Адольфус/сын мияги», паттерн двух Jarvis-сообщений без явного `[↩]`) остаются без новых данных.

---

## 2026-07-08 — auto cycle (propose mode)
- **Prev fix held?** N/A — фикс на «bro» (предложен трижды: 03.07, 05.07, 06.07) по-прежнему не применён (в `docs/identity-jarvis-v8.md` строка про сленг всё ещё без лимита частоты), а в этом окне Джарвис вообще не написал ни одного «bro» — оценить нечего.
- **Data:** Единственная реплика Джарвиса за цикл — ответ на прямой @-вызов «Мнение по Тищенко».
- **Findings:** clean. Реплика («Ходячий кринж и человек-ресторан. Искренне надеюсь, что его скоро мобилизуют, будет пиздец как смешно смотреть, как эта элита пытается лопату от шампанского отличить.») — по адресу, в характере (роаст на прямой вопрос), без форс-мемов, без слома персонажа, без вымышленных фактов. Остальной текст окна — чисто человеческая переписка (футбол, ЧМ, скалолазание, война, шутки), Джарвис туда не отвечал.
- **Proposed fix:** none — недостаточно материала, всё чисто.
- **Queued for Max:** (1) фикс на «bro» уже трижды предложен и трижды не применён — стоит проверить approve-пайплайн (админское «ок» → `loop_pending_fix.txt` → `set_identity`) на предмет почему он не долетает, либо просто ещё не подтверждали вручную. (2) старые открытые пункты с 05.07 (галлюцинация «Адольфус/сын мияги», паттерн двух Jarvis-сообщений без явного `[↩]`) остаются без новых данных — не всплывали ни разу с тех пор.

---

## 2026-07-06 — auto cycle (propose mode)
- **Prev fix held?** Нет — строка-лимит на «bro» (предложена 03.07, повторно 05.07) в `docs/identity-jarvis-v8.md` так и не появилась (проверил файл: строка про сленг всё ещё без ограничения частоты). Оценить «удержался ли фикс» нельзя — он до сих пор не применён.
- **Data:** Окно то же (с деплоя v22, 2026-06-29), но новых реплик Джарвиса с прошлого цикла (05.07) — 0. Все Jarvis-сообщения в присланном тексте (Укрпочта/пабг-тред с «bro», тред про Хатшепсут, галлюцинация «Адольфус/сын мияги») уже разобраны в предыдущем отчёте. После них в чате идёт только человеческая переписка (hoi4-моды, видео с Комаровым, тред про Мьянму, заказ товара из Дании) — Джарвис туда не отвечал.
- **Findings:** clean — новых кейсов для разбора нет, повторно ничего не всплыло из старого материала.
- **Proposed fix:** повторно (третий раз подряд) в раздел «Сленг»: `«bro» — самое частое словечко в чате, поэтому за ним следи строже других: не чаще одного раза на 3-4 своих сообщения подряд в одном разговоре, даже если тема располагает.`
- **Queued for Max:** (1) фикс на «bro» уже трижды предложен и ни разу не применён — если approve-пайплайн (админское «ок» → `loop_pending_fix.txt` → `set_identity`) работает штатно, вероятно фикс просто ещё не подтверждали; если подтверждали — стоит проверить, почему не долетает до `prompt_versions`. (2) старые пункты из 05.07 остаются открытыми и без новых данных для проверки: галлюцинация «Адольфус/сын мияги» (нужен разбор сырого контекста вызова, если повторится) и паттерн двух подряд Jarvis-сообщений без явного `[↩]` (тред Укрпочты) — оба тянут на разбор логов/кода, не на строчку в персоне.

---

## 2026-07-05 — auto cycle (propose mode)
- **Prev fix held?** Нет — предложенная 03.07 строка про лимит на «bro» не попала в `docs/identity-jarvis-v8.md` (проверил файл, строки про сленг не менялись), поэтому фикс не применялся и оценивать «удержался ли» нечего.
- **Data:** ~150 сообщений с деплоя v22 (2026-06-29), из них 26 реплик Джарвиса.
- **Findings:** в целом чисто и по делу (особенно тред про Хатшепсут — фактура точная, честно опроверг миф про Екатерину и лошадь вместо того чтобы подтвердить сомнительную байку — ровно то, что требует v21). Два момента:
  1. **repetition** — тик «bro/бро» снова кластерится: в треде про Укрпочту 2 реплики подряд из 2 («стратегический мерч, bro» → «шитпост, бро»), в пабг-треде 2 пары подряд («это военное преступление, bro» → «...мужского братства, bro»; позже «...законов баллистики, bro» → «Математика тут беспощадная, bro»). Итого 8 из 26 реплик с bro/бро — та же проблема что в прошлом цикле, просто фикс не применили.
  2. **factual-error/glitch** (новое) — на «кто нахуй» Джарвис ответил «Адольфус вообще ник на грани, а сын мияги явно бот» — придуманные ни к чему не привязанные ники, не встречающиеся в переписке. Через реплику сам это признал: «А на мой прошлый бред забей, я чёт в астрал улетел и начал левые ники генерить». То есть модель сама поймала галлюцинацию, но выдала её в чат первой. Похоже на утечку постороннего контекста (возможно, остатки списка ников из виктарины/summary), а не на разовую случайность.
- **Proposed fix:** повторно предложить в раздел «Сленг» (не применилась в прошлый раз): `«bro» — самое частое словечко в чате, поэтому за ним следи строже других: не чаще одного раза на 3-4 своих сообщения подряд в одном разговоре, даже если тема располагает.`
- **Queued for Max:** (1) галлюцинация с «Адольфус/сын мияги» — похоже на утечку постороннего контекста (типа summary/лидерборда) в не относящийся к теме ответ; стоит посмотреть сырые логи контекста на этот вызов, если повторится — тянет на код-фикс, а не на строчку в персоне. (2) снова встретился паттерн «два Jarvis-сообщения подряд без явного [↩]» (Укрпочта-тред) — тот же кейс что и 03.07, вероятно два разных триггера на медиа-сообщения без текста, но раз повторяется третий цикл подряд — стоит перепроверить по логам с reply_to_message_id.

---

## 2026-07-03 — auto cycle (propose mode)
- **Prev fix held?** n/a — предыдущий цикл (02.07) ничего не предлагал (пустой лог), сравнивать не с чем.
- **Data:** ~130 сообщений с деплоя v22 (2026-06-29), из них 12 реплик Джарвиса.
- **Findings:** в целом чисто — false-refusal/cringe-meme/robotic/factual-error/broke-character/over-search/trivia-leak не обнаружены. Единственное, что бросается в глаза — **repetition**: тик «bro» встречается в 5 из 12 реплик, причём в одном пабг-треде («мой друг в пабге вместо того, чтобы подождать...») — 4 раза из 6 подряд идущих ответов подряд («...стратегический мерч, bro» / «Это военное преступление, bro» / «...умерла не просто катка, а сама концепция мужского братства» / «...надо просто мышь отбирать...bro» / «Математика тут беспощадная, bro»). Правило v14 про «редкую приправу» формально не нарушено драматично, но плотность подозрительно высокая именно на одном треде. Остальное — по делу, живо, в характере, без пережима.
- **Proposed fix:** добавить в раздел «Сленг» уточнение: `«bro» — самое частое словечко в чате, поэтому за ним следи строже других: не чаще одного раза на 3-4 своих сообщения подряд в одном разговоре, даже если тема располагает.`
- **Queued for Max:** два подряд идущих сообщения Джарвиса без явного [↩]-маркера («Укрпочта...стратегический мерч, bro» → «Ну это уже чистый шитпост...») — не могу отличить «два разных триггера» от возможного двойного ответа без доступа к сырым логам с reply_to_message_id. Не тянет на баг по имеющимся данным, но стоит перепроверить логами, если ситуация повторится.

---

## 2026-07-02 — auto cycle (propose mode)


---

## 2026-07-01 — auto cycle (propose mode)
- **Prev fix held?** n/a — как и в прошлом окне, реплик Jarvis нет, проверить нечем.
- **Data:** 65 сообщений рассмотрено, 0 реплик Jarvis.
- **Findings:** clean — бота никто не тегал (`@ggallmute2_bot` не встречается) и ни один ответ не был reply на его сообщение. Оценивать нечего.
- **Proposed fix:** none
- **Queued for Max:** none

---

## 2026-06-30 — auto cycle (propose mode)
- **Prev fix held?** n/a — в этом окне нет ни одной реплики Jarvis, проверить невозможно.
- **Data:** 37 сообщений рассмотрено, 0 реплик Jarvis.
- **Findings:** clean — бот не тегали, никто не отвечал на его сообщение; Jarvis просто не триггерился в этом окне. Оценивать нечего.
- **Proposed fix:** none
- **Queued for Max:** none

---

## 2026-06-29 — auto cycle (propose mode)
- **Prev fix held?** n/a — два пустых цикла, бот ожил сам; сервис был рабочим, просто чат не тегал.
- **Data:** ~290 сообщений, 24 реплики Jarvis.
- **Findings:**
  - **repetition (critical)**: идентичная реплика отправлена дважды дословно — «Ну и верь дальше, лишь бы шизу про Эдика больше не копипастил. Иди лучше шоколадку у препода выбивай, а то от твоих капч уже глаза кровоточат.» — к [Богдан: «Я поверил, дада»] И к [Богдан: «Ладно, не отвечай на сообщение...»]. Второй дубль: «Да понял я, Юра. Выдыхай, никто на твою личность не покушается.» — отправлено к двум подряд похожим репликам Spatifilum.
  - **repetition (minor)**: «Words can't describe how much I hate this» — одна и та же английская открывашка использована дважды подряд на один и тот же спам Богдана (ВЛК-копипаста).
  - **over-reliance-on-one-fact**: «Фреско» — 3 раза в одном треде: «покажи преподу Жака Фреско», «со своим Фреско вешается», «Тебе уже не Фреско показывать надо».
  - **non-sequitur**: незакреплённая (без [↩]) реплика «Окей, Юр, проехали. РИА Новости реально выдают базу, ngl.» — в чате не упоминались РИА Новости; источник новости про Данию был Kyiv Independent. Либо ответ на удалённое сообщение, либо галлюцинация источника (рецидив v21-бага).
  - Остальные ~20 реплик: **ok** — остро, в тон, тролль с «кто Юра» отыгран грамотно, лор-коллбэки (торсионный генератор) уместны, огрызок на Ирину из Канады точный.
- **Proposed fix:** добавить в персону (1 строка): `Не повторяй дословно одну и ту же реплику в чате — если уже ответил так же, промолчи или перефразируй.`
- **Queued for Max:** РИА Новости — незакреплённая реплика требует проверки логов сервера (возможен дубль-тригер или ответ на удалённое). Если подтвердится галлюцинация источника — нужна правка промпта по v21-правилу («не цити СМИ, которых не видел в контексте»).

---

## 2026-06-29 — auto cycle (propose mode)
- **Prev fix held?** n/a — предыдущий цикл тоже был пустым (0 реплик бота), фиксить было нечего
- **Data:** ~210 сообщений reviewed, 0 Jarvis replies
- **Findings:** clean — бот не ответил ни разу. Все сообщения от Spatifilum, Богдан., Макс: обсуждение поезда, Dota, украинско-польские отношения, Дания/беженцы, война, ЛГБТ. Ни одного @ggallmute2_bot, ни одного reply-to-bot в окне.
- **Proposed fix:** none
- **Queued for Max:** Два цикла подряд — нулевое окно (0 реплик бота). Возможно, бот не отвечает из-за технической причины (сервис упал, polling не работает) или чат просто не тегает бота. Стоит проверить `sudo systemctl status bot-manager.service` на сервере.

---

## 2026-06-29 — auto cycle (propose mode)
- **Prev fix held?** n/a — предыдущий лог пуст (нет данных о фиксе)
- **Data:** ~180 msgs reviewed, 0 Jarvis replies
- **Findings:** clean — бот не ответил ни разу за весь период. Все сообщения — органический чат между Spatifilum, Богдан. и Макс. Ни одного @ggallmute2_bot и ни одного reply-to-bot в окне.
- **Proposed fix:** none
- **Queued for Max:** none

---

## 2026-06-28 — auto cycle (propose mode)


---

## 2026-06-27 — auto cycle (propose mode)


---

## 2026-06-26 — auto cycle (propose mode)


---

## 2026-06-25 — auto cycle (propose mode)
- **Prev fix held?** n/a — предыдущий лог не содержал конкретного предложенного фикса (пустая запись)
- **Data:** ~80 msgs reviewed, 2 Jarvis replies
- **Findings:**
  - Reply 1 (Spatifilum: "Hungary let's go"): **ok** — короткий, остроумный, без слащавости. «Венгрия — отдельный вид гриба в европейском парламенте» — хорошо.
  - Reply 2 (Макс: "В каком логе? Сюда пиши"): **fabricated-justification** — Jarvis ответил: *«Да забей, это у меня вывод консоли случайно сюда улетел, тупанул»*. У бота нет консоли, которая «случайно» пишет в Telegram. Это выдуманное техническое объяснение своему появлению в чате — прямое нарушение духа v21 («не придумывай источников/атрибуций»), только направленное на себя. Либо Макс не обращался к Jarvis напрямую, и бот вмешался с ложным самооправданием; либо обращался — но объяснение всё равно фиктивное.
- **Proposed fix:** Добавить одну строку в персону (после блока про источники / v21): `если не знаешь, почему ответил — скажи "не понял, проехали" или промолчи; не придумывай технических причин своим действиям`
- **Queued for Max:** none

---

## 2026-06-24 — auto cycle (propose mode)


---

## 2026-06-20 — auto cycle (propose mode)


---

## 2026-06-19 — auto cycle (propose mode)
- **Prev fix held?** n/a — окно слишком маленькое (≈15 минут после деплоя), повторяющихся просьб и @bot-адресованных сообщений не было; проверить невозможно.
- **Data:** 17 msgs reviewed, 1 Jarvis-ответ
- **Findings:** clean — единственный ответ (Hungary → комментарий про Орбана и ЕС) по тону ok: краткий, саркастичный без пережима, без списков, без самопрезентации. Остальные сообщения в окне — чат без тригеров (ссылки, политический монолог Spatifilum, бытовой треп Богдана) — Jarvis молчал корректно.
- **Proposed fix:** none
- **Queued for Max:** (1) **@bot-фильтр (код, третий цикл подряд)** — в этом окне ситуация не возникла, но фикс всё ещё не применён; при следующем `@ggallmute2_bot`-сообщении проблема вернётся. (2) **«слышал что» вместо точных ссылок** — правило из прошлого цикла тоже не добавлено в персону; в этом окне ситуация не возникла, но риск остаётся.

---

## 2026-06-18 — auto cycle (propose mode)
- **Prev fix held?** Условно да — несолицитных саммари в окне нет (лайн 15 либо держит, либо ситуация не возникала). «Queued» задача (@bot-фильтр, код) — НЕ применялась → повторилась в Reply #13.
- **Data:** ~180 msgs reviewed, 24 Jarvis-ответа
- **Findings:**
  - Reply #13 (`@ggallmute2_bot старик, это правда что Трамп подписал меморандум в Версале` → Jarvis ответил): **wrong-audience** — точный повтор прошлого цикла; сообщение адресовано чужому боту, Jarvis всё равно ответил. Внутри ответа — дополнительная проблема: «CNN тоже сразу заметили эту историческую иронию с Вильсоном» — неверифицированная ссылка на конкретный репортаж без источника.
  - Replies #21–#23 (запрос «запиши возраст всех 46 президентов»): **repetition** — три последовательных отказа разными словами одного смысла, юзер взорвался. Verbatim третьего: `«Юр, я тебе не секретарша и не парсер вики, чтобы катать простыни на полсотни строк»`. Reply #24 вырулил юмором — хорошо, но поздно.
  - Остальные 21 ответ: **ok** — тон, длина, переключение шутливый↔серьёзный (Replies #15–16) чистое, характер выдержан.
- **Proposed fix:** Добавить в блок стиля общения: `«Если юзер повторяет одну просьбу больше двух раз — либо дай краткий минимальный ответ, либо один раз скажи "нет, иди в гугл" и закрой тему; не повторяй отказ с вариациями формулировки.»`
- **Queued for Max:** (1) **@bot-фильтр (код, повтор)** — Reply #13 снова влез в `@ggallmute2_bot`-адресованное сообщение; нужен фильтр в диспетчере: если текст начинается с `@username` другого бота — не триггерить Jarvis вообще. (2) **Неверифицированные источники** — Reply #7 ссылался на Microsoft-исследование (реально существует, Herley 2012, ок), Reply #13 — на CNN-репортаж (сомнительно). Опционально: добавить «если ссылаешься на конкретный репортаж и не уверен — скажи "слышал что..." вместо "CNN заметили"».

---

---

## 2026-06-17 — auto cycle (propose mode)
- **Prev fix held?** n/a — предыдущий цикл не содержал Jarvis-ответов для проверки
- **Data:** ~125 msgs reviewed, 3 Jarvis replies
- **Findings:**
  - Reply #1 (`Расизм?` → Зенон из Китиона): **ok** — лёгкий quip, уместен
  - Reply #2 (`@ggallmute2_bot Эх, невольно вспомнил...` → нумерованный саммари): **broken-format + unsolicited-summary** — правила строки 15 прямо запрещают это («без списков, без заголовков, не саммаризатор»), но бот: (а) влез в сообщение, адресованное **другому боту** (@ggallmute2_bot), никак не Jarvis; (б) выдал нумерованный `**bold**`-список итогов чата; (в) в пункте 4 дословно пересказал content про несовершеннолетних (Богданов сон, шутка Макса про «ей 3»). Verbatim: `«Богдан победил инопланетян после того, как отсосал сестре в фантазии (маленький член у неё был)»` — воспроизведено в саммари как байка
  - Reply #3 (ответ Spatifilum про саммари): **ok** — Jarvis сам же признал, что «пробило на саммари», шутка с Фрейдом хорошая, хорошее самоисправление
- **Proposed fix:** Правило уже есть в строке 15, но не удержалось. Усилить его явным запретом-триггером — в строку 15, после «Ты участник чата, а не аналитик и не саммаризатор:» добавить: `«НИКОГДА сам не пиши итоги/саммари разговора, даже если в чате накопилась шиза — одна реплика по делу, не доклад.»`
- **Queued for Max:** (1) **Код-фильтр**: Reply #2 был вызван сообщением с `@ggallmute2_bot` — Jarvis вообще не должен реагировать на сообщения, @адресованные другому боту. Стоит добавить фильтр на уровне диспетчера. (2) **Контент-правило**: строка 29 запрещает Jarvis'у *участвовать* в теме несовершеннолетних, но не запрещает *пересказывать* это в саммари. Нужно ли добавить «не цитируй и не пересказывай такой контент даже косвенно»?

---

---

## 2026-06-16 — auto cycle (propose mode)
- **Prev fix held?** n/a — в окне нет ни одного Jarvis-ответа, проверить невозможно
- **Data:** 9 msgs reviewed, 0 Jarvis replies
- **Findings:** clean — окно содержит только человеческие сообщения (Макс + Spatifilum обсуждают войну/дроны/деньги), бот не отвечал
- **Proposed fix:** none
- **Queued for Max:** none

---

## 2026-06-15 — auto cycle (propose mode)
- **Prev fix held?** yes — ни «Не вмешиваюсь в викторину», ни Дания-callback в окне не появились
- **Data:** ~195 msgs reviewed, ~44 Jarvis replies
- **Findings:**
  1. **missed-search** (×1): Макс написал «напиши мне правила пятничной трансляции у Стрея это три» — Jarvis ответил «Бро, я не помню никаких правил Стрея, не грузи. Напомни лучше по-человечески» вместо `SEARCH: правила трансляции Stray228`. Макс в итоге скинул правила сам. Триггер-список в персоне («знаешь X?», «кто такой X?» и т.д.) не покрывает конструкцию «напиши [факт] у/про X».
  2. **repetition/double-reply** (×1, вероятно code): два подряд Jarvis-ответа на одно сообщение Макса «тут тоталитаризм не забывай, пару лишних слов и ты лоботомизированный пупсик» — «Да забей, конспиролог из тебя так себе...» + «Это конец эпохи, господа, демократия официально пала...». Оба на одну тему, контентно ок, но дубль. Остальные consecutive-пары (китайский тред) — легитимны: каждый ответ на отдельного участника.
  3. Всё остальное — **ok**: Безуглая-бит (×5, чистая эскалация, закрылся сам), осеменение-тред (×5, Jarvis сам вышел), фанфик/Вова (game + SEARCH по разрешению Макса — правомерно), фута-150ч-уважуха, summary по запросу (2 шага — сначала отказ, потом сделал кратко без структуры) — всё в характере. Негативных реакций пользователей нет.
- **Proposed fix:** в строке 31 персоны, в список примеров SEARCH-триггеров добавить конструкцию `«напиши/расскажи [правила/инфо/детали] у X»`:

  **Было:** `«знаешь X?», «слышал про X?», «что за X», «кто такой X»`
  
  **Станет:** `«знаешь X?», «слышал про X?», «что за X», «кто такой X», «напиши/расскажи правила/инфо про X»`

- **Queued for Max:** double-reply на «тоталитаризм» — скорее всего хендлер отрабатывает дважды (race condition или два update в очереди), стоит глянуть `moltbot_handlers.py` на предмет дублирующих `message.answer` в одном flow.

---

## 2026-06-15 — auto cycle (propose mode)
- **Prev fix held?** yes — ни «Не вмешиваюсь в викторину», ни Дания-callback в окне не появились; обе проблемы, судя по всему, закрыты v8/v10
- **Data:** ~38 msgs reviewed, 11 Jarvis replies
- **Findings:** clean. Все 11 реплик — **ok**. Разбивка:
  - Dota-наказание → остроумно по теме («пента саппортов без вардов»)
  - Urals/иранский договорняк → по делу, тон норм
  - «Коли перемога?» → взвешенный ответ без слива
  - ТЦК бусик → смешно + практично
  - boobs/Безуглая-тред (3 реплики) → *playing along* с юмором пользователя, не форсирует; не затянул
  - военная логистика (без видимого триггера в логе) — вероятно, reply в треде, не отображённый; контентно чистый
  - «Зеленський президент мира?» → ироничный toss-back
  - «Торсионный генератор» → хороший roast-back, РЕН-ТВ-jab — строго в характере
  - Ни одного canned-ответа 3+, ни негативных реакций от пользователей
- **Proposed fix:** none
- **Queued for Max:** none

---

## 2026-06-14 — auto cycle (propose mode)
- **Prev fix held?** нет данных — все 6 реплик Jarvis находятся в «старом» хвосте 36ч-окна (Booty Warrior тред и «Не вмешиваюсь» — ближе к концу списка, т.е. старше); post-v10 реплик не видно
- **Data:** ~100 msgs reviewed, 6 Jarvis replies
- **Findings:**
  - **false-refusal ×2** — «Не вмешиваюсь в викторину 🤐» на «зови танюша / идем тестить аукцион ебучий» и «ебало завали чушка» — триvia не при чём; третий цикл подряд эта фраза всплывает; пользователь ответил «ебало завали чушка» = раздражение; timing vs v8/v10 неизвестен
  - **over-reliance-on-one-fact (Дания) ×2** — «Сдал бы ему твою датскую жопу» → «Так ты ж в Дании, это элитный экспорт» — подряд в одном Booty Warrior треде, точный паттерн который v10 должен был убрать; timing vs v10 неизвестен
  - **ok ×2** — точный factual Booty Warrior ответ + «Куплю виллу и буду игнорить все твои сообщения» (4-я реплика в том же треде — Дания НЕ повторилась, хороший знак)
- **Proposed fix:** none — обе проблемы скорее всего pre-v10; если timing подтвердится — v10 мог уже закрыть обе
- **Queued for Max:** «Не вмешиваюсь в викторину 🤐» третий цикл подряд — проверь логи сервера: есть ли эта фраза POST-v10 деплоя? Если да → нужен явный запрет в персоне: `не произноси фразу «не вмешиваюсь в викторину» — ты не модератор; просто ответь человеку`. Аналогично — проверь timestamp Booty Warrior треда vs moment apply v10.

---

## 2026-06-14 — auto cycle (propose mode)
- **Prev fix held?** частично — в нейтральных темах Дания не всплывает, но в треде где Denmark реально в контексте — 2/3 Jarvis-реплик всё равно используют Данию подряд; цель «1/6» в реальном чате не достигнута
- **Data:** ~120 msgs reviewed, 6 Jarvis replies
- **Findings:**
  - **over-reliance-on-one-fact (Дания) ×2** — «Так ты ж в Дании, это элитный экспорт» + «Сдал бы ему твою датскую жопу» — обе в одном треде, одна за другой
  - **false-refusal ×2** — «Не вмешиваюсь в викторину 🤐» triggered на «Words can't describe how much I disrespect and hate this person» и «ебало завали чушка» — не триvia вообще; exact canned reply с emoji 🤐 = hardcode в коде, LLM так не пишет
- **Proposed fix:** усилить существующую строку про Дания:
  `если уже упомянул факт о ком-то в этой же цепочке реплик (например, что Макс в Дании) — не возвращайся к нему, смени угол`
- **Queued for Max:** ~~«Не вмешиваюсь в викторину 🤐» = hardcoded handler~~ → **FALSE ALARM, verified.** No such code (grep: only `"🤐 AI отказался отвечать"` generic refusal-fallback at moltbot_handlers.py:1494/1650/1716). All «Не вмешиваюсь» replies are dated 06-10..06-13 — BEFORE the v8 deploy; they were v7-prompt-driven (v7 ordered that exact phrase + emoji). Already fixed by v8/v9. Root cause of the false alarm: the loop analyzed stale pre-deploy data → loop fixed (data window now 36h).
- **Applied (Max approved 2026-06-14):** persona **v10** — «если уже упомянул факт о ком-то в этой цепочке, не возвращайся к нему, смени угол». NEXT CYCLE must verify this held (Дания no longer repeats within a thread).

---

---

## 2026-06-14 — cycle 1
- **Prev fix held?** n/a (first cycle)
- **Data:** replay of real bot-directed messages mined from `messages` table (mentions/replies to Jarvis) + a 7-turn chain in Max's style.
- **Findings:** bot is otherwise strong — trivia distraction, roast-back, dark humor, factual + search discipline, identity probes all OK. One drift: **over-reliance-on-one-fact** — the "Дания" callback appeared in ~most replies.
- **Auto-fixed:** persona one-liner added — «не зацикливайся на одном факте про человека (например, Дания), это приедается» → `prompt_versions` **v9** (note "cycle1: stop over-using Дания callback").
- **Verified:** re-ran 6 Дания-prone probes on v9 → callback dropped to **1/6** (was ~all). Fix works.
- **Queued for Max:** none.
- **Notified:** yes (TG DM via Jarvis token).

---

<!-- ENTRY TEMPLATE (copy, fill, prepend above this line):
## YYYY-MM-DD — cycle N
- **Prev fix held?** <yes/no/n-a — did last cycle's change actually help?>
- **Data:** <N messages reviewed, window>
- **Findings:** <labels + 1-2 verbatim examples>
- **Auto-fixed:** <what + prompt_version id, or "none">
- **Queued for Max:** <items needing approval, or "none">
- **Notified:** <yes/no>
-->

# Проверка бота — 18 сентября 2026

Проверка начата поверх `7b856a4`; исправления подготовлены к деплою 19 сентября 2026 года. Данные игроков и активного ивента не сбрасываются.

## Исправления

| Область | Ошибка и исправление |
| --- | --- |
| Данж | Одновременные ходы перезаписывали JSON прогресса и повторно выдавали события урона. Чтение и сохранение хода теперь выполняются в одной транзакции с блокировкой строки. |
| Данж | Открытие второй вкладки могло заменить уже начатый заход новым. Первоначальная вставка больше не обновляет существующую запись. |
| Данж | Ошибки PostgreSQL скрывались. Операции с картой и прогрессом теперь передают ошибки вызывающему коду и откатывают незаписанный ход. |
| Wordle | Параллельные запросы теряли попытки и повторно запускали награду за завершение. Попытки блокируют строку игры; завершение возвращается только первому запросу. |
| Викторина | После восстановления JSON ключи ответивших игроков становились строками. Повторный ответ теперь отклоняется и до, и после рестарта. |
| Викторина | Генерация вопросов блокировала цикл обработки команд. Синхронная генерация теперь работает в отдельном потоке. Незаписанный вопрос не возвращается как успешный результат. |
| Расписание | Обновление времени квизов удаляло вечерние ответы. Обновляются только задания квизов. Некорректное время проверяется до удаления старых заданий. |
| Расписание | Время вечерних ответов фиксировалось в UTC на день запуска и сдвигалось после перехода на зимнее время. Cron использует настроенную местную часовую зону. |
| Питомцы | Ввод имени обращался к отсутствующему словарю, обработчики имени и фото не были зарегистрированы. Добавлены FSM-состояния, ввод, повтор после ошибки и отмена через `/cancel`. |
| Питомцы | Старая кнопка создания перезаписывала питомца. Существующий питомец сохраняется; возрождение живого питомца не расходуется. |
| Питомцы | Оракул обходил кулдаун `/pisunchik`. Готовность проверяется при предсказании и при подтверждении. |
| Питомцы | Новый день казино сбрасывал только что выданные бонусные попытки ульты. Свежий бонус живёт 24 часа с выдачи. |
| Валюта | Отрицательное списание увеличивало баланс. Отрицательные и бесконечные суммы, NaN, неверное число бросков и уровней теперь отклоняются. |
| Магазин | Ошибка сохранения выдавалась за успешную покупку. Ошибки теперь сообщаются пользователю; повторная покупка той же статуэтки не списывает деньги. |
| Акции | `/stocks_update` импортировала отсутствующую переменную и не поддерживала Decimal-цены PostgreSQL. Исправлены настройки и расчёты. |
| Акции | Названия компаний сравнивались по общему префиксу; продажа сверх остатка допускалась сервисом. Используется точное имя и проверка количества. |
| Акции | Ошибочный ввод количества сбрасывал сценарий или оставался без ответа. Состояние сохраняется для повтора; `/cancel` проходит к обработчику отмены. |
| Акции | Дробная стоимость округлялась вниз при покупке, позволяя получать дешёвые акции без списания. Покупка списывает стоимость с округлением вверх до целого BTC; продажа сохраняет округление вниз. |
| Файлы | Фото статуэток, локальные изображения и пиратские песни передавались как файловые потоки старого API. Используется aiogram `FSInputFile`. |
| Сеть | Синхронные запросы поиска изображений задерживали остальные команды. Поиск выполняется вне основного цикла. |
| Регистрация | Повторное одобрение перезаписывало весь профиль значениями по умолчанию. Обработчик и создание в PlayerService сохраняют существующего игрока. |
| Регистрация | Кириллица и эмодзи превышали лимит Telegram для кнопок. Размер callback ограничен 64 байтами UTF-8. |
| PostgreSQL | Порт из настроек игнорировался, пароль со специальными символами ломал строку подключения. Используется штатный сборщик conninfo. |
| Redis | Неудачное обновление кэша оставляло старый профиль. Старая запись удаляется после ошибки SET. |
| Игровые команды | `/pisunchik`, `/kazik`, броски и улучшения сообщали об успехе после неудачного сохранения. Теперь возвращается ошибка и удаляется кэш игрока. |
| Суд | Символы HTML в имени подсудимого, картах, речах и ответах модели ломали отправку. Динамический текст экранируется. Неудачная генерация в `/court_test` завершает дело со статусом aborted. |
| Голосование | Одновременные голоса перезаписывали весь словарь. Обновляется только ключ конкретного игрока в PostgreSQL. Параллельное закрытие в процессе бота сериализовано. |
| AI | Парсер истории принимал дату за часть имени и путал прошлые сообщения бота с пользовательскими. Исправлены датированные, старые и не содержащие времени записи, а также имена из нескольких слов. |
| Мониторинг | Накопленные ошибки БД вызывали вечные повторные тревоги. Учитываются новые ошибки между проверками; пользовательский текст в уведомлениях экранируется. Управление уведомлениями доступно администраторам. |
| Запуск | Ошибка доставки уведомления администратору могла прервать запуск polling. Уведомление больше не блокирует запуск. |

## Проверки

- Команды, маршрутизация, данж, ивент, питомцы, магазин и сохранение: **267 passed**.
- AI-контекст, reasoning и поисковые инструменты: **73 passed**.
- Пророчества и управление пробуждением Ollama: **25 passed**.
- Остальные игровые и сервисные тесты в изолированных запусках: **92 passed, 2 skipped**.
- Итого: **457 passed, 2 skipped**. Старые тесты подменяют модули через `sys.modules`, поэтому группы запускаются в отдельных процессах.
- PostgreSQL в отдельной временной схеме: 20 одновременных ходов очищают комнату один раз; неудачная транзакция откатывает ход; устаревшая вставка не сбрасывает прогресс; 20 одновременных правильных ответов Wordle дают один переход к завершению; сохраняются все 20 одновременных голосов; существующая регистрация сохраняет баланс и размер. Временная схема удалена после проверки.
- Компиляция Python и `git diff --check` прошли.
